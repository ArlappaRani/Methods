package staticandnonstaticmethods;

public class StaticProgram2 {
	/*In this program we calling the static method and also it is 
	 having two different classes so we call the method using class
	 name with method name
	 */
     public static String Display()
     {
    	 return "Inside display";
     }
}
